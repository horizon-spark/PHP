<?php 
    function getMax($a, $b) {
        return $a > $b ? $a : $b;
    }

    function getMin($a, $b) {
        return $a < $b ? $a : $b;
    }

    function getAverage($a, $b) {
        return ($a + $b) / 2;
    }
?>